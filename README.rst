anime-podcast
=============

ローカル開発を続行する前に、以下の実行環境を用意してください。

- 最低要件: Node.js 20 以上
- 最低要件: Python 3.11 以上

このリポジトリには開発時の既定バージョンとして ``.nvmrc`` (Node.js 20) と
``.python-version`` (Python 3.11) を配置しています。

確認コマンド:

.. code-block:: bash

   node -v
   python3 --version